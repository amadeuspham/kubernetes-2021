apiVersion: v1
kind: Service
metadata:
  name: {{ service_name }}
  labels:
    dummysite: "{{ dummysite_name }}"
spec:
  type: ClusterIP
  selector:
    app: {{ dummysite_name }}
  ports:
    - port: {{ service_port }}
      protocol: TCP
      targetPort: 3000